{
  "name": "reverse-dns-service",
  "version": "1.0.0",
  "description": "A micro service for acquiring the computer name with given ip.",
  "main": "dist/server.js",
  "scripts": {
    "start": "tsc && node ./dist/server.js"
  },
  "author": "Yoav Rozner",
  "license": "ISC",
  "dependencies": {
    "@types/amqplib": "^0.5.13",
    "amqplib": "^0.5.5",
    "dns": "^0.2.2",
    "typescript": "^3.8.3"
  }
}
